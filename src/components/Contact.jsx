import { brand } from '../data/brandData.js'
const labels={instagram:'Instagram',facebook:'Facebook',tiktok:'TikTok',whatsapp:'WhatsApp / Phone',website:'Website'}
export default function Contact(){const verified=Object.entries(brand.contacts).filter(([,v])=>v);return <section id="contact" className="section contact"><div className="container contact__inner"><p className="eyebrow">Contact & order enquiry</p><h2>Ready for Your Next Shift?</h2><p>Connect with MK Medi Care through an official channel to ask about ordering and availability.</p>{verified.length?<div className="contact-links">{verified.map(([k,v])=><a key={k} href={v}>{labels[k]}</a>)}</div>:<div className="contact-pending"><span aria-hidden="true">MK</span><p>Verified contact channels will be listed here soon.</p></div>}</div></section>}
