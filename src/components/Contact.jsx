import { brand } from '../data/brandData.js'
export default function Contact(){const verified=Object.values(brand.contacts);return <section id="contact" className="section contact"><div className="container contact__inner"><p className="eyebrow">Contact & order enquiry</p><h2>Ready for Your Next Shift?</h2><p>Connect with MK Medi Care through an official channel to ask about ordering and availability.</p><div className="contact-links">{verified.map(item=><a key={item.label} href={item.href}><span>{item.label}</span><strong>{item.display}</strong></a>)}</div></div></section>}
